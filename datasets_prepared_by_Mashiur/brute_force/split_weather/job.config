10 # max allowed unique category item in a feature column (for not ignoring it)
3 # num_threads
0 # D, if less than D rows, create a subtree with one thread
1 # task batch size: how many tasks to fetch by a thread from task queue each time
1 # NO_WARNING: 1 = true, 0 = false
dataset/brute_force/split_weather/train0 #training data
dataset/brute_force/split_weather/test0 #test data
dataset/brute_force/split_weather/meta.csv #meta data
4 # which column is y?
job # directory for putting job logs and tree outputs